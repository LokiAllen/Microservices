Please ensure each of the 4 microservices add_song.py convert_frag.py list_songs.py remove_song.py are running
Ensure a valid ENV.txt key is present for the audd.io API
Finally ensure the folder called wavs is present containing all .wav files supplied on ele
Run python -m unittest discover to run all tests